<div class="row">
  <div class="col-auto">
    <h2>Hello, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h2>Author a New Blog Post:</h2>

    <form class="form new-post-form">
      <div class="form-group">
        <label class="mb-3 pr-2" for="title">Title:</label>
        <input class="form-input" type="text" id="title" name="title" />
      </div>
      <div class="form-group">
        <label for="article">Article:</label>
        <textarea class="form-input" id="article" name="article"></textarea>
      </div>
      <div class="form-group mt-3">
        <button type="submit" class="btn btn-primary">Create</button>
      </div>
    </form>
  </div>

  {{#if posts.length}}
  <div class="col-md-6 post-list">
    <h3>Current Articles:</h3>

    {{#each posts as |post|}}
    <div class="row mb-2 post_comments">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.title}}</a></h4>
        <p>{{post.contents}}</p>
      </div>
      <div class="col-md-4 deletes pt-2">
        <button id ="delButton" class="btn btn-secondary btn-sm" data-id="{{post.id}}">Delete</button>

         <button id = "updateButton" class="btn btn-sm btn-secondary updates" data-id="{{post.id}}">Update</button>  
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<script src="./js/dashboard.js"></script>
